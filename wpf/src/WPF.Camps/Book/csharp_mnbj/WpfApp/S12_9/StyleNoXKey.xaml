<Window x:Class="S12_9.StyleNoXKey"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:S12_9"
        mc:Ignorable="d"
        Title="不指定x:key的资源样式"  Height="350" Width="525">
    <Window.Resources>
        <!--不设置x:Key的值，则在资源集合有效范围内的所有可视化对象都会自动套用资源中的样式-->
        <Style TargetType="{x:Type Rectangle}">
            <Setter Property="Fill" Value="LightGreen"/>
            <Setter Property="Stroke" Value="Blue"/>
            <Setter Property="StrokeThickness" Value="3.5"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="135"/>
        </Style>
    </Window.Resources>
    <WrapPanel Margin="25" Orientation="Horizontal">
        <Rectangle/>
        <Rectangle/>
        <Rectangle/>
    </WrapPanel>
</Window>
