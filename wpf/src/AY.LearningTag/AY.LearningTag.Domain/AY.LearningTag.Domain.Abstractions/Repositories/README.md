Repositories目录中定义的接口用于抽象化数据访问层，提供对不同数据存储技术的统一访问方式。
通过这些接口，应用程序可以与各种数据库（如关系型数据库、NoSQL数据库等）进行交互，而无需关心底层实现细节。

注意：Domain.Abstractions中提供的接口，与具体的数据库工具无关。即可以使用sqlHelper、Dapper等工具实现这些接口，也可以使用EFCore等ORM框架实现这些接口。
